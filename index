<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Dein Name – Portfolio</title>
  <meta name="description" content="Portfolio von Dein Name – Media, Design & Produktion." />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    /* ====== Design-Variablen ====== */
    :root{
      --bg: #0b0c0f;           /* sehr dunkles Grau mit Blaustich */
      --bg-soft: #11131a;      /* Karten-Hintergrund */
      --text: #e6e8ef;         /* Primärtext */
      --text-dim: #b2b7c6;     /* Sekundärtext */
      --accent: #7aa2ff;       /* Akzentfarbe */
      --muted: #2a2f3a;        /* Linien/Border */
      --ring: #9ab6ff;         /* Fokusring */
      --maxw: 1100px;
      --radius: 16px;
      --shadow: 0 10px 25px rgba(0,0,0,.35), 0 2px 8px rgba(0,0,0,.25);
    }

    @media (prefers-color-scheme: light){
      :root{
        --bg: #f9fafb;
        --bg-soft: #ffffff;
        --text: #0d1321;
        --text-dim: #4b5563;
        --accent: #3b82f6;
        --muted: #e5e7eb;
        --ring: #93c5fd;
      }
    }

    /* ====== Reset & Base ====== */
    *,*::before,*::after{ box-sizing:border-box }
    html{ scroll-behavior:smooth }
    body{
      margin:0; font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background: var(--bg); color: var(--text); line-height:1.65; -webkit-font-smoothing: antialiased;
    }
    img{ max-width:100%; display:block; }
    a{ color:inherit; text-decoration:none }
    a:hover{ color: var(--accent) }
    .container{ width:100%; max-width: var(--maxw); margin-inline:auto; padding: 0 20px; }
    .tag{ display:inline-flex; align-items:center; gap:.5rem; border:1px solid var(--muted); padding:.35rem .6rem; border-radius:999px; font-size:.85rem; color:var(--text-dim) }

    /* ====== Header / Nav ====== */
    header{
      position: sticky; top:0; z-index:20; backdrop-filter: blur(8px);
      background: color-mix(in oklab, var(--bg) 75%, transparent);
      border-bottom: 1px solid var(--muted);
    }
    .nav{ display:flex; align-items:center; justify-content:space-between; padding:14px 0; }
    .brand{ display:flex; align-items:center; gap:.75rem; font-weight:700; letter-spacing:.3px }
    .dot{ width:10px; height:10px; border-radius:50%; background: var(--accent); box-shadow: 0 0 16px var(--accent) }
    .menu{ display:flex; gap:1.2rem; }
    .menu a{ color:var(--text-dim); font-weight:500 }

    /* ====== Hero ====== */
    .hero{ padding: 72px 0 36px }
    .hero-inner{ display:grid; grid-template-columns: 1.2fr .8fr; gap:40px; align-items:center }
    .hero h1{ font-size: clamp(2.2rem, 3.6vw + 1rem, 3.6rem); line-height:1.12; margin: 0 0 14px; letter-spacing:.2px }
    .hero p{ font-size: clamp(1rem, .4vw + .9rem, 1.15rem); color: var(--text-dim); margin:0 0 22px }
    .actions{ display:flex; gap:.8rem; flex-wrap:wrap }
    .btn{
      display:inline-flex; align-items:center; gap:.6rem; padding:.75rem 1rem; border-radius: 999px; border:1px solid var(--muted);
      background: var(--bg-soft); color: var(--text); font-weight:600; letter-spacing:.2px; box-shadow: var(--shadow);
    }
    .btn.primary{ background: linear-gradient(135deg, var(--accent), color-mix(in oklab, var(--accent) 70%, #fff 30%)); border-color: transparent; color: #0b1020 }
    .btn:hover{ transform: translateY(-1px); filter: saturate(1.05) }

    .card{ background: var(--bg-soft); border:1px solid var(--muted); border-radius: var(--radius); box-shadow: var(--shadow) }

    .portrait{ aspect-ratio: 4/5; overflow:hidden }
    .portrait img{ width:100%; height:100%; object-fit:cover; filter: grayscale(35%) contrast(1.05) }

    /* ====== Sections ====== */
    section{ padding: 56px 0 }
    .section-head{ display:flex; align-items:end; justify-content:space-between; gap:1rem; margin-bottom: 22px }
    h2{ font-size: clamp(1.6rem, 1.5vw + 1rem, 2.2rem); margin:0 }
    .sub{ color: var(--text-dim); font-size: .98rem }

    /* ====== Projekte ====== */
    .grid{ display:grid; gap:18px }
    .projects{ grid-template-columns: repeat(12, 1fr) }
    .project{
      grid-column: span 12; padding:16px; display:grid; grid-template-columns: 1fr 1.2fr; gap:16px; align-items:center
    }
    .project h3{ margin:.2rem 0 .35rem }
    .meta{ display:flex; gap:.5rem; flex-wrap:wrap; margin:.35rem 0 .8rem }
    .meta .tag{ border-color: var(--muted); color: var(--text-dim) }
    .project a.more{ font-weight:600; color: var(--accent) }

    /* ====== Skills ====== */
    .skills{ display:flex; flex-wrap:wrap; gap:.6rem }
    .chip{ padding:.5rem .7rem; border:1px dashed var(--muted); border-radius:10px; color:var(--text-dim); font-weight:600; font-size:.9rem }

    /* ====== Footer ====== */
    footer{ padding: 36px 0 80px; border-top:1px solid var(--muted); color: var(--text-dim) }

    /* ====== Responsiv ====== */
    @media (max-width: 920px){
      .hero-inner{ grid-template-columns: 1fr; }
      .project{ grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  <!-- ====== Header ====== -->
  <header>
    <div class="container nav">
      <a class="brand" href="#top"><span class="dot" aria-hidden="true"></span> <span>Dein&nbsp;Name</span></a>
      <nav class="menu" aria-label="Hauptnavigation">
        <a href="#projekte">Projekte</a>
        <a href="#about">Über mich</a>
        <a href="#kontakt">Kontakt</a>
        <a href="https://github.com/DEIN-GITHUB" target="_blank" rel="noreferrer noopener">GitHub</a>
      </nav>
    </div>
  </header>

  <!-- ====== Hero ====== -->
  <main id="top" class="hero">
    <div class="container hero-inner">
      <div>
        <span class="tag" aria-label="Rolle"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 2a10 10 0 1 0 10 10A10.011 10.011 0 0 0 12 2Zm1 15h-2v-2h2Zm0-4h-2V7h2Z"/></svg> Media Conception & Produktion</span>
        <h1>Ich gestalte klare, elegante Medienprodukte.</h1>
        <p>Schwerpunkt auf Konzept, Bildsprache und präziser Umsetzung. Hier findest du ausgewählte Projekte, meinen Werdegang und Kontaktmöglichkeiten.</p>
        <div class="actions">
          <a class="btn primary" href="#projekte">Meine Arbeiten ansehen</a>
          <a class="btn" href="DeinCV.pdf" target="_blank" rel="noopener">Lebenslauf</a>
        </div>
      </div>
      <div class="card portrait">
        <img src="https://images.unsplash.com/photo-1544006659-f0b21884ce1d?q=80&w=1200&auto=format&fit=crop" alt="Portrait von Dir" />
      </div>
    </div>
  </main>

  <!-- ====== Projekte ====== -->
  <section id="projekte">
    <div class="container">
      <div class="section-head">
        <h2>Ausgewählte Projekte</h2>
        <p class="sub">3–6 Highlights mit kurzer Beschreibung</p>
      </div>

      <div class="grid projects">
        <!-- Projekt 1 -->
        <article class="card project">
          <div>
            <img src="https://images.unsplash.com/photo-1526948128573-703ee1aeb6fa?q=80&w=1200&auto=format&fit=crop" alt="Projektvorschau 1" />
          </div>
          <div>
            <h3>Projektname Eins</h3>
            <p>Kurzer, prägnanter Teaser: Aufgabe, meine Rolle, Ergebnis/Impact. Max. 2 Sätze.</p>
            <div class="meta">
              <span class="tag">Konzept</span>
              <span class="tag">Regie</span>
              <span class="tag">Postproduktion</span>
            </div>
            <a class="more" href="#" target="_blank" rel="noopener">Case Study öffnen →</a>
          </div>
        </article>

        <!-- Projekt 2 -->
        <article class="card project">
          <div>
            <img src="https://images.unsplash.com/photo-1501785888041-af3ef285b470?q=80&w=1200&auto=format&fit=crop" alt="Projektvorschau 2" />
          </div>
          <div>
            <h3>Projektname Zwei</h3>
            <p>Welche Herausforderung? Was war meine Lösung? Was hat es gebracht?</p>
            <div class="meta">
              <span class="tag">Kampagne</span>
              <span class="tag">Art Direction</span>
              <span class="tag">Video</span>
            </div>
            <a class="more" href="#" target="_blank" rel="noopener">Mehr sehen →</a>
          </div>
        </article>

        <!-- Projekt 3 -->
        <article class="card project">
          <div>
            <img src="https://images.unsplash.com/photo-1520975916090-3105956dac38?q=80&w=1200&auto=format&fit=crop" alt="Projektvorschau 3" />
          </div>
          <div>
            <h3>Projektname Drei</h3>
            <p>Ein Satz zum Ziel, ein Satz zu deinem Beitrag. Fertig.</p>
            <div class="meta">
              <span class="tag">Branding</span>
              <span class="tag">Design</span>
              <span class="tag">Foto</span>
            </div>
            <a class="more" href="#" target="_blank" rel="noopener">Details →</a>
          </div>
        </article>
      </div>
    </div>
  </section>

  <!-- ====== Über mich ====== -->
  <section id="about">
    <div class="container">
      <div class="section-head">
        <h2>Über mich</h2>
        <p class="sub">Kurz & auf den Punkt</p>
      </div>
      <div class="card" style="padding:20px;">
        <p><strong>Hi, ich bin Dein Name</strong> – Media Conception & Production (HSRM). Ich arbeite an der Schnittstelle von Idee, Ästhetik und Umsetzung. Besonders mag ich klare Bildsprache, schlanke Prozesse und starke Story-Arcs.</p>
        <p><strong>Für wen ich arbeite:</strong> Kurzfilm, Social/Brand Content, Event & Musik. Offen für Kollaborationen in Frankfurt/Rhein-Main & remote.</p>
        <div class="skills" aria-label="Skills">
          <span class="chip">Konzeptentwicklung</span>
          <span class="chip">Regie</span>
          <span class="chip">Kamera</span>
          <span class="chip">Schnitt</span>
          <span class="chip">Color</span>
          <span class="chip">DaVinci Resolve</span>
          <span class="chip">Premiere Pro</span>
          <span class="chip">After Effects</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ====== Kontakt ====== -->
  <section id="kontakt">
    <div class="container">
      <div class="section-head">
        <h2>Kontakt</h2>
        <p class="sub">Lass uns über dein Projekt sprechen</p>
      </div>
      <div class="card" style="padding:20px; display:grid; gap:12px;">
        <p><strong>E-Mail:</strong> <a href="mailto:dein.name@mail.com">dein.name@mail.com</a></p>
        <p><strong>Instagram:</strong> <a href="https://instagram.com/deinhandle" target="_blank" rel="noopener">@deinhandle</a></p>
        <p><strong>LinkedIn:</strong> <a href="https://www.linkedin.com/in/deinprofil" target="_blank" rel="noopener">/in/deinprofil</a></p>
      </div>
    </div>
  </section>

  <footer>
    <div class="container" style="display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:12px;">
      <span>© <span id="year"></span> Dein Name</span>
      <span style="color:var(--text-dim)">gebaut mit reinem HTML & CSS</span>
    </div>
  </footer>

  <script>
    // Jahreszahl im Footer
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
